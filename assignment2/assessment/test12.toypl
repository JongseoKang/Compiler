func main()
const a := 1, b := 2
var c, d, e, f, g, h i,
begin
  c 33;
  c <- (71:<-b)
  d <- (a + b);
  e <- c - d;
  f <- (c * 3);
  g <- (c / d);
  h <- (g + (e + c) * (2 - 4) 5 / (10 - d));
  print(h);
end
